<%-
  mem = num_cores.blank? ? 4 : num_cores.to_i * 4

-%>

---
batch_connect:
  template: vnc
script:
  job_name: "matlab_ood_test"
  native:
    - "-l"
    - "select=1:ncpus=<%= num_cores.blank? ? 1 : num_cores.to_i %>:mem=<%= mem %>gb"

